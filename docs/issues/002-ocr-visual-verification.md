# 对易混公式和化学符号执行定向图片核对

## 问题

真实包 `context_000012` 多轮生成和反思均保留 `Hz`，原表内容疑似 `H2`。当前提示词要求图片核对，但模型在没有明确视觉核对候选时不会稳定定位易混字符。

## 期望

- 对字母/数字、上下标、化学式、公式符号建立程序化视觉核对候选。
- 当 OCR 文本出现 `Hz/H2`、`O/0`、`l/1` 等高风险模式时，把对应图片局部和候选位置明确传给反思阶段。
- 图片仍不可辨时保留未决状态，不猜测替换。

## 验收标准

- `context_000012` 能主动报告并核对 `Hz/H2`。
- 修正必须有图片证据；无法确认时不得静默写入。
- 回归测试覆盖普通文本、表格和公式三类易混字符。

