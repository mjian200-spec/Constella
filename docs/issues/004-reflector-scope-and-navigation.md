# 限制 Full 反思补入导航信息和扩大约束作用域

## 问题

固定同一批 v11 初稿的 40 包 A/B 中，Full 整体优于 Main，但仍有两类回归：

- `context_000390`：重新补入 `图4-44展示其他接头形式`，这是文档导航而非领域规则。
- `context_000172`：把一般性的 Ar 中加入 He/H2 规律补入只适用于 TIG 的规则组，扩大了 C 的作用域。

## 期望

- 图号、表号、“见图”“可供参考”和文档位置不得作为规则对象或结果。
- ADD_R 前必须证明当前组 C 覆盖新增规则；不能证明时新建正确作用域的组或不补。
- 资源存在仅表示可核对，不自动要求补规则。

## 验收标准

- `context_000390` 不生成图号导航规则。
- `context_000172` 的一般规律不受 TIG 专项 C 错误限制。
- 使用真实资源可选、导航句和跨段约束案例回归。

