# 语义对齐局部试验记录（2026-08-30）

## 范围

- 概念来源：`outputs/article_concepts_full_20260829`，720个概念。
- 规则来源：`outputs/rule_extraction_full_20260829`，17,565条规则、6,166个去重对象。
- 模型：本地`/DATA/jm/llms/qwen3.8-27b`，temperature=0，关闭thinking。
- 对齐方法：字符n-gram TF-IDF只负责候选召回和分包，LLM是唯一语义裁决器。
- 试验输出：`outputs/semantic_alignment_trial_20260830`（运行缓存，不进入Neo4j）。

## 最小协议

1. 概念融合：LLM仅输出需要合并的概念ID组。
2. 对象对齐：LLM仅输出`对象ID -> 概念ID/NEW/REPARSE/INVALID`。
3. 状态规范化：LLM仅输出同义状态ID组及一条包含规范概念名的完整状态，或异常枚举。

包和结果均以输入及提示词指纹缓存；成功包可直接恢复，失败包自动重试一次。

## 真实局部结果

### 概念融合

- 3/3包成功，协议成功率100%，裁决覆盖率100%。
- LLM提出4组合并，人工复核4/4为明确同义：
  - `MIG焊` / `MIG`
  - `阳极压降` / `阳极电压降`
  - `焊缝深宽比` / `焊缝的深宽比`
  - `自保护型药芯焊丝` / `自保护药芯焊丝`
- 当前只证明观察到的合并精确率，不能据此证明全量召回率。

### 对象对齐

- 3/3包成功，45/45个高频对象被裁决。
- 26个对齐已有概念，16个需要补概念，3个需要重解析，0个不可恢复。
- 协议成功率100%，裁决覆盖率100%，直接可用率93.33%。
- `极性`和`熔滴过渡形式`等仍是需要在全量结果中重点抽查的边界项。

### 状态规范化

- 最终使用20条状态/包，并在同一概念内按TF-IDF近邻聚包。
- 4/4个“飞溅”状态包成功，67/67条状态被裁决。
- 63条完成规范化，4条标记为`WRONG_CONCEPT`。
- 协议成功率100%，裁决覆盖率100%，直接规范化率94.03%。
- 人工复核4条异常均正确：`主要原因`、`效果显著`、`大于120A时`、`比120A大`。
- 近邻聚包修复了两个120A同义异常被分到不同包后判断不一致的问题。

## 当前门槛与风险

三阶段局部协议均达到100%成功和覆盖，已具备启动全量概念融合的条件。仍需在扩大运行中持续统计：

- 概念合并误合并率和漏合并率；
- 对象`NEW`比例及新概念重复率；
- `REPARSE`对象的二次解析覆盖率；
- 状态异常精确率、跨包规范状态重复率；
- 模型超时、截断和协议修复重试率。

在对象全量结束后，新建概念必须再次经过概念融合；`REPARSE`对象必须完成逐状态二次解析，不能直接导入Neo4j。
