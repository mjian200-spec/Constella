# 项目说明

## 项目信息

- 项目名称：Constella
- 项目用途：从焊接技术资料中提取可追溯的焊接知识，并用于焊接参数推荐。
- 主要语言或框架：Python 3.11+、PyYAML、Pillow、Neo4j；前端使用原生 HTML/CSS/JavaScript；模型通过 OpenAI 兼容接口调用。
- 源码目录：`src/constella/`
- 配置目录：`configs/`
- 提示词目录：`prompts/`
- 本地输入和运行产物：`GMAW/`、`outputs/`，默认提交 Git。

## 常用入口

- `scripts/build_context_packages.py`：从 MinerU 文档解析结果构建上下文。
- `scripts/extract_rules.py`：提取焊接规则。
- `scripts/build_concept_layer.py`：从规则构建焊接概念层。
- `scripts/discover_article_concepts.py`：直接从文章上下文发现概念和关系。
- `scripts/serve_review.py`：查看上下文构建结果。
- `scripts/serve_rule_review.py`：查看和审核规则抽取结果。
- `scripts/test_concept_layer_real.py`：运行 Concept Layer 真实数据验收。

所有命令默认从仓库根目录执行。具体参数先查看对应脚本的 `--help`、README 和配置文件，不要假定模型服务或 Neo4j 已经启动。

## 工作方式

- 开始任务前，先阅读与需求相关的文件。
- 修改前，先说明准备修改哪些文件以及原因。
- 优先使用最小改动方案，不做与当前任务无关的重构。
- 先运行 `git status --short`，保留用户尚未提交的修改，不覆盖、不回滚无关内容。
- 搜索文件和文本优先使用 `rg --files` 和 `rg`。
- 需求不明确且不同选择会实质改变结果时，先说明假设并向用户确认。
- 能从仓库、配置、脚本或测试中确认的信息，不反问用户。
- 不虚构不存在的文件、命令、测试、服务和执行结果。
- 诊断任务默认只定位和说明原因；用户明确要求修复时再修改代码。

## 修改要求

- 每次任务只修改完成目标所必需的文件。
- 不随意调整目录结构、批量重命名、批量格式化或清理历史资源。
- 不安装或升级依赖，除非先说明必要性并得到确认。
- 保持项目现有代码、提示词和视觉风格。
- 修改提示词时，检查提示词 ID、版本、缓存和相关测试。
- 修改输出格式时，同时检查写入端、读取端、Viewer 和恢复逻辑。
- 前端修改后，需要检查电脑端和手机端；无法进行视觉检查时明确说明。

## Git 提交要求

- 完成修改并通过必要检查后，将本次改动提交到当前 Git 分支；不要只保留在工作区。
- 提交前再次运行 `git status --short` 和 `git diff --check`，只暂存本次任务涉及的文件，不混入用户已有改动或无关产物。
- 提交标题应简洁说明目标，可以使用英文，也可以使用中文。
- 提交正文必须使用中文，并用分条形式写明具体改动内容；每条说明一个明确改动，不使用笼统表述。
- 提交正文至少覆盖核心实现改动和相应验证或测试改动；仅修改文档时，应分条说明修改了哪些规范或说明。
- 不执行远程推送；只有用户明确要求时才运行 `git push`。

## 安全边界

- 不读取、输出或提交 `.env`、API Key、Token、Cookie、助记词、数据库密码和其他私有凭据。
- 不输出环境变量全集；检查单个变量时也不得打印其值。
- 不访问项目目录之外的个人文件。
- 不删除或覆盖 `GMAW/`、`outputs/`、SQLite、WAL、人工反馈或其他重要资源，除非得到明确确认。
- 不执行数据库迁移、Neo4j 清图、生产部署、远程推送和权限修改，除非得到明确确认。
- 不访问生产服务器和生产数据库。
- 常规规则验证优先使用 `--no-graph` 或 `--dry-run-resolve`，不要把 `--reset-graph` 当作测试参数。

## 验证要求

完成修改后：

1. 列出所有新增、修改和删除的文件。
2. 说明每个文件的改动。
3. 运行与任务最相关的测试或检查。
4. 报告实际执行的命令和结果。
5. 说明没有运行的检查以及原因。
6. 说明可能存在的剩余风险。
7. 说明需要人工确认的地方。

常用检查命令：

```bash
python -m compileall -q src/constella
ruff check --select F,E9 src/constella tests
PYTHONPATH=src pytest -q
git diff --check
```

- 单模块修改先运行对应测试，再根据风险决定是否运行完整测试。
- 仅修改文档时检查内容、路径和格式即可，通常不需要运行代码测试。
- 真实模型、Neo4j、真实数据或前端视觉检查未执行时必须明确说明。

## 输出习惯

- 默认使用中文，先给结论，再给必要细节。
- 路径、文件名和命令使用反引号。
- 不把“建议运行”写成“已经运行”。
- 无法验证时明确说明，不假装完成。
- 不把已有用户改动算作本次修改。

---

规范结构参考：Keta的随身听，知乎回答：<https://www.zhihu.com/question/2067268243996275512/answer/2076673976256967087>。项目内容已按 Constella 当前情况改写。
